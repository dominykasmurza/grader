# Contributing

Contributions are welcome. Use synthetic data only. Do not upload real examinations, candidate data, answer keys, or protected logos.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
pip install pytest
pytest
```

Open an issue before substantial changes and document user-facing behavior.
